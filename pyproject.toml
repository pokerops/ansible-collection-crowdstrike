[project]
name = "pokerops-crowdstrike"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
  "pokerops-ansible-utils@git+https://github.com/pokerops/ansible-utils@master",
  "crowdstrike-falconpy>=1.5.3",
  "github3-py>=4.0.1",
  "jmespath>=1.0.1",
]

[tool.setuptools]
packages = []
